{
  "name": "myblog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --d1 DB",
    "deploy": "wrangler pages deploy .",
    "db:migrate": "wrangler d1 execute myblog-db --file=schema.sql",
    "db:seed": "wrangler d1 execute myblog-db --file=seed.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
